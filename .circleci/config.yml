jobs:
  test:
    executor:
      docker:
        - image: cimg/base:stable
    steps:
      - checkout
      - node/install-packages
      - run:
          command: npm run test  -- --coverage
version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
      - test
