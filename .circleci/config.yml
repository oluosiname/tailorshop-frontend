jobs:
  test:
    docker:
      - image: circleci/node@3.0.0
      tag: "13"
    steps:
      - checkout
      - node/install-packages
      - run:
          command: npm run test  -- --coverage
version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
      - test
